run = "sh -c 'pip install -U pip && pip install -r requirements.txt && streamlit run app.py --server.port $PORT --server.address 0.0.0.0'"
language = "python"
onBoot = "echo Starting Streamlit on port $PORT"

